# Copy to .env.local for the Next.js app.
# Leave unset in local dev — client-side calls proxy to localhost:8000
# via next.config.ts, and server-side calls fall back to it directly.
NEXT_PUBLIC_API_URL=
# Server-side-only override (SSR/build), if the backend isn't on localhost:
INTERNAL_API_URL=
